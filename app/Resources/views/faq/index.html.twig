{% extends 'layoutAdmin.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-center title-style">FAQ</h1>
        <div class="row">
            <div class="col-md-3 offset-9">
                <a class="btn btn-outline-primary mb-3" href="{{ path('faq_admin_new') }}">+ Ajouter une question</a>
            </div>
        </div>

        <table class="table table-striped table-bordered text-center">
            <thead>
            <tr>
                <th>Question</th>
                <th>Réponse</th>
                <th></th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for faq in faqs %}
                <tr>
                    <td>{{ faq.question }}</td>
                    <td>{{ faq.response}}</td>
                    <td>
                        <li>
                            <a class="btn btn-outline-primary" href="{{ path('faq_admin_show', { 'id': faq.id }) }}">Voir</a>
                        </li>
                    </td>
                    <td>
                        <li>
                            <a class="btn btn-outline-secondary" href="{{ path('faq_admin_edit', { 'id': faq.id }) }}">Modifier</a>
                        </li>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
