{% extends 'layoutAdmin.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Faq {{ faq.id }}</h1>
        <div class="row">
            <div class="col-9">
                <table class="table table-bordered" >
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ faq.id }}</td>
                    </tr>
                    <tr>
                        <th>Question</th>
                        <td>{{ faq.question }}</td>
                    </tr>
                    <tr>
                        <th>Réponse</th>
                        <td>{{ faq.response }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
            <div class="col-3 border-left">
                <ul>
                    <li>
                        <a class="btn btn-outline-secondary" href="{{ path('faq_admin_index') }}">Liste des faq</a>
                    </li>
                    <li>
                        <a class="btn btn-outline-primary" href="{{ path('faq_admin_edit', { 'id': faq.id }) }}">Modifier</a>
                    </li>
                    <li>
                        {{ form_start(delete_form) }}
                        <input class="btn btn-outline-danger" type="submit" value="Supprimer">
                        {{ form_end(delete_form) }}
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}



