{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Ajouter un partenaire</h1>
        <hr>
        <div class="row">
            <div class="col-8"></div>
        </div>
        <div class="row">
            <div class="col-6">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input class="btn btn-outline-primary" type="submit" value="Créer" />
                {{ form_end(form) }}
            </div>
            <div class="col-6 border-left">
                <a class="btn-light " href="{{ path('partnair_index') }}">Liste des partenaires</a>
                {% for partnair in partnairs %}
                    <ul>
                        <li>
                            <a href="{{ path('partnair_show', { 'id': partnair.id }) }}">{{ partnair.name }}</a>
                            <a href="{{ path('partnair_edit', { 'id': partnair.id }) }}">
                            <input class="btn btn-outline-primary btn-sm" type="submit" value="edit" /></a>
                        </li>
                    </ul>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
