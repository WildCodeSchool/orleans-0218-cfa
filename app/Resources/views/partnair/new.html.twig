{% extends 'layoutAdmin.html.twig' %}
{% block title %}Administration des partenaires du cfa de Blois{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter un partenaire</h1>
        <div class="row">
            <div class="col-8">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input class="btn btn-outline-primary" type="submit" value="Créer" >
                {{ form_end(form) }}
            </div>
            <div class="col-4 border-left">
                <a class="btn btn-outline-secondary" href="{{ path('partnair_index') }}">Liste des partenaires</a>
            </div>
        </div>
    </div>
{% endblock %}
