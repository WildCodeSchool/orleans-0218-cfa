{% extends 'layoutAdmin.html.twig' %}

{% block body %}
    <h1>Cfa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cfa.id }}</td>
            </tr>
            <tr>
                <th>Le mot du président</th>
                <td>{{ cfa.textPresident }}</td>
            </tr>
            <tr>
                <th>Historique</th>
                <td>{{ cfa.historical }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cfa_admin_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cfa_edit', { 'id': cfa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
