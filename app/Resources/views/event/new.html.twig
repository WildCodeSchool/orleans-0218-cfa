{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Création d'évènement</h1>
        <div class="row">
            <div class="col-8">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input class="btn btn-outline-primary" type="submit" value="Créer" />
            {{ form_end(form) }}
            </div>
            <div class="col-4 border-left">
                <a class="btn-lg btn-success" href="{{ path('event_index') }}">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
