{% extends 'layoutAdmin.html.twig' %}

{% block body %}
    <div class="container">
    <h1>Liste des taxes</h1>
        <div class="row">
            <div class="col-md-3 offset-9">
                {% if taxes is empty %}
                    <a class="btn btn-outline-primary mb-3" href="{{ path('cfa_admin_new') }}">+ Créer</a>
                {% endif %}
            </div>
        </div>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Taxe d'apprentissage</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for taxe in taxes %}
            <tr>
                <td><a href="{{ path('taxe_show', { 'id': taxe.id }) }}">{{ taxe.taxeApprentissage | raw }}</a></td>
                <td><a class="btn btn-outline-primary" href="{{ path('taxe_show', { 'id': taxe.id }) }}">Voir</a></td>
                <td><a class="btn btn-outline-secondary" href="{{ path('taxe_edit', { 'id': taxe.id }) }}">Modifier</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}
