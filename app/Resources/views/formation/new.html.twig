{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Créer une formation</h1>
            <div class="row">
                <div class="col-8">
                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <input class="btn-lg btn-primary" type="submit" value="Créer" />
                    {{ form_end(form) }}
                </div>
            <div class="col-4">
                <a class="btn-light btn-lg" href="{{ path('formation_index') }}">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
